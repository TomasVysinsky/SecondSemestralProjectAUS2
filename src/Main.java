public class Main {
    public static void main(String[] args) {
        new View();

//        Tests test = new Tests();
//        test.testIRecordByteConversions();
//        test.monteCarlo(1);

/*
        if (test.bruteForceTestInsert()) {
            System.out.println("Test insert vysiel dobre");
        } else {
            System.out.println("Test insert vysiel zle");
        }

        long start = System.nanoTime();
        if (test.bruteForceTestFind()) {
            System.out.println("Test find vysiel dobre");
        } else {
            System.out.println("Test find vysiel zle");
        }
        double duration1 = (System.nanoTime() - start) / 1000000;

        System.out.println("Aktualne zdravie stromu je: " + test.getHealth() + "%");

        test.changeDepth(120);
        test.changeDepth(70);
        test.changeDepth(90);

        test.optimise();

        start = System.nanoTime();
        if (test.bruteForceTestFind()) {
            System.out.println("Test find 2 vysiel dobre");
        } else {
            System.out.println("Test find 2 vysiel zle");
        }
        double duration2 = (System.nanoTime() - start) / 1000000;

        System.out.println("Aktualne zdravie stromu je: " + test.getHealth() + "%");

        System.out.println("Find after optimalisation took " + ((duration1 / duration2) * 100) + "% of time of the find before the optimalisation.");
        System.out.println("Find after optimalisation took " + (100 - (duration1 / duration2) * 100) + "% of time of the find before the optimalisation.");

        if (test.bruteForceTestDelete()) {
            System.out.println("Test delete vysiel dobre");
        } else {
            System.out.println("Test delete vysiel zle");
        }*/
    }
}